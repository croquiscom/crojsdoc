ul.nav.nav-list.cormo-sidenav
  li.nav-header
    | Classes
  each klass_name in classes
    if klass_name===name
      li
        a(href='#'+klass.html_id)= klass_name
      each property in properties
        li.cormo-class-property
          a(href='#'+property.html_id)= property.ctx.name
    else
      li
        a(href=klass_name+'.html')= klass_name
